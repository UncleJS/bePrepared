/**
 * (auth)/layout.tsx — standalone layout for auth pages.
 * Auth pages use this wrapper inside the root layout.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
